{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev":"npx @11ty/eleventy --serve & npx tailwind -i src/style.css -o dist/style.css --watch",
    "build":"npx @11ty/eleventy && NODE_ENV=production npx tailwind -i src/style.css -o dist/style.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^2.2.19"
  }
}
